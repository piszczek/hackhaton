{% form_theme form 'themes/section_form.html.twig' %}

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="mdl-button mdl-js-button mdl-button--raised">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
