{% block body %}
    {# @var section \App\Entity\Section #}
    <div class="top">
        <div class="inner">
            Section: <strong>{{ section.name }}</strong> <br> <small>Turned on</small>
        </div>
    </div>
    <div class="content">
        <div class="inner">
            {{ include('section/_form.html.twig', {'button_label': 'Update'}) }}
            <a href="{{ path('section_index') }}">Back to list</a>
            {{ include('section/_delete_form.html.twig') }}
        </div>
    </div>
    <script>
        window.componentHandler.upgradeAllRegistered();
    </script>
{% endblock %}
